git add .
git commit -m "messeage"
git push origin <branch>
-------tao branch
git branch <branchname> 
-------chuyen branch
git checkout <branch> 
-------xoa branch
git branch -d <branchname> 
-------xoá folder git ở repo
git rm -r one-of-the-directories
git commit -m "Remove duplicated directory"
git push origin <your-git-branch> (typically 'master', but not always)
------xoá ko xoá folder ở local
git rm -r --cached myFolder
git commit -m "Removed folder from repository"
git push origin master
------merge
git checkout test //chuyen branch
git pull origin master //pull từ master
git push origin test
git checkout master
git pull origin test 
OR
git checkout test
git pull origin master
git checkout master
git merge test
-------------------
Tạo Branch mới từ 1 Branch khác trong Git
---Chuyển tới branch mà bạn đang cần tạo mới từ nó :
git checkout develop
---Tạo branch mới :
git checkout -b develop_1 develop
---Push dữ liệu tới branch mới :
git push origin develop_1

Kiểm tra lại danh sách branch đang sử dụng:
git branch -a
